{
  "schema_version": 1,
  "events": [
    {
      "key": "integration.test",
      "category": "integration",
      "description": "Manual connectivity and payload test.",
      "default_message": "🧪 **Manual connectivity and payload test**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "system.update.started",
      "category": "system",
      "description": "A controlled server operation was requested and queued.",
      "default_message": "🛠️ **A controlled server operation was requested and queued**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "system.update.result",
      "category": "system",
      "description": "A controlled server operation completed or failed.",
      "default_message": "🛠️ **A controlled server operation completed or failed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "system.maintenance.started",
      "category": "system",
      "description": "A maintenance window was activated.",
      "default_message": "🛠️ **A maintenance window was activated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "system.maintenance.ended",
      "category": "system",
      "description": "A maintenance window ended or failed.",
      "default_message": "🛠️ **A maintenance window ended or failed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "backup.run.requested",
      "category": "operations",
      "description": "An administrator requested a protected application backup.",
      "default_message": "💾 **An administrator requested a protected application backup**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "backup.restore.requested",
      "category": "operations",
      "description": "A bootstrap administrator requested a database restore.",
      "default_message": "💾 **A bootstrap administrator requested a database restore**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "backup.configuration.updated",
      "category": "operations",
      "description": "The protected backup connection configuration changed.",
      "default_message": "💾 **The protected backup connection configuration changed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "backup.configuration.deleted",
      "category": "operations",
      "description": "The protected backup connection configuration was removed.",
      "default_message": "💾 **The protected backup connection configuration was removed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "privacy.request.created",
      "category": "moderation",
      "description": "A data-subject request requires an administrator response.",
      "default_message": "🛡️ **A data-subject request requires an administrator response**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "privacy.request.resolved",
      "category": "moderation",
      "description": "A data-subject request was resolved by an administrator.",
      "default_message": "🛡️ **A data-subject request was resolved by an administrator**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "registration.request.created",
      "category": "registrations",
      "description": "A new access request was submitted.",
      "default_message": "🔔 **A new access request was submitted**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "registration.request.approved",
      "category": "registrations",
      "description": "An access request was approved.",
      "default_message": "🔔 **An access request was approved**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "registration.request.rejected",
      "category": "registrations",
      "description": "An access request was rejected.",
      "default_message": "🔔 **An access request was rejected**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.created",
      "category": "squads",
      "description": "A squad was created.",
      "default_message": "⚓ **A squad was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.updated",
      "category": "squads",
      "description": "A squad was updated.",
      "default_message": "⚓ **A squad was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.archived",
      "category": "squads",
      "description": "A squad was archived.",
      "default_message": "⚓ **A squad was archived**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.member.added",
      "category": "squads",
      "description": "A member was added to a squad.",
      "default_message": "⚓ **A member was added to a squad**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.member.updated",
      "category": "squads",
      "description": "A squad membership was updated.",
      "default_message": "⚓ **A squad membership was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "squad.member.removed",
      "category": "squads",
      "description": "A member was removed from a squad.",
      "default_message": "⚓ **A member was removed from a squad**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "group.created",
      "category": "groups",
      "description": "A new group-search listing was created.",
      "default_message": "🔎 **A new group-search listing was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "group.member.joined",
      "category": "groups",
      "description": "A member joined a group-search listing.",
      "default_message": "🔎 **A member joined a group-search listing**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "group.closed",
      "category": "groups",
      "description": "A group-search listing was closed.",
      "default_message": "🔎 **A group-search listing was closed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "calendar.event.created",
      "category": "calendar",
      "description": "A fleet or squad event was created.",
      "default_message": "📅 **A fleet or squad event was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "calendar.event.updated",
      "category": "calendar",
      "description": "A fleet or squad event was updated.",
      "default_message": "📅 **A fleet or squad event was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "calendar.event.cancelled",
      "category": "calendar",
      "description": "A fleet or squad event was cancelled.",
      "default_message": "📅 **A fleet or squad event was cancelled**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "guide.created",
      "category": "content",
      "description": "A new guide was published.",
      "default_message": "📘 **A new guide was published**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "guide.updated",
      "category": "content",
      "description": "A published guide was updated.",
      "default_message": "📘 **A published guide was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "guide.removed",
      "category": "content",
      "description": "A guide was removed from publication.",
      "default_message": "📘 **A guide was removed from publication**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "newcomer_guide.updated",
      "category": "content",
      "description": "The starter guide was updated.",
      "default_message": "📘 **The starter guide was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "build.created",
      "category": "builds",
      "description": "A new build was created.",
      "default_message": "🧰 **A new build was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "build.updated",
      "category": "builds",
      "description": "A build was updated.",
      "default_message": "🧰 **A build was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "build.removed",
      "category": "builds",
      "description": "A build was removed.",
      "default_message": "🧰 **A build was removed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "build.printout.published",
      "category": "builds",
      "description": "A public build printout was published to Discord.",
      "default_message": "🧰 **A public build printout was published to Discord**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.thread.created",
      "category": "forum",
      "description": "A new forum thread was created.",
      "default_message": "💬 **A new forum thread was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.thread.updated",
      "category": "forum",
      "description": "A forum thread was updated.",
      "default_message": "💬 **A forum thread was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.thread.removed",
      "category": "forum",
      "description": "A forum thread was removed.",
      "default_message": "💬 **A forum thread was removed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.post.created",
      "category": "forum",
      "description": "A new forum reply was posted.",
      "default_message": "💬 **A new forum reply was posted**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.post.updated",
      "category": "forum",
      "description": "A forum reply was updated.",
      "default_message": "💬 **A forum reply was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "forum.post.removed",
      "category": "forum",
      "description": "A forum reply was removed.",
      "default_message": "💬 **A forum reply was removed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.created",
      "category": "fleet",
      "description": "A fleet was created.",
      "default_message": "🏴‍☠️ **A fleet was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.updated",
      "category": "fleet",
      "description": "A fleet profile was updated.",
      "default_message": "🏴‍☠️ **A fleet profile was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.application.created",
      "category": "fleet",
      "description": "A fleet application was submitted.",
      "default_message": "🏴‍☠️ **A fleet application was submitted**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.membership.updated",
      "category": "fleet",
      "description": "A fleet membership was updated.",
      "default_message": "🏴‍☠️ **A fleet membership was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.leader.assigned",
      "category": "fleet",
      "description": "A fleet leadership role was assigned.",
      "default_message": "🏴‍☠️ **A fleet leadership role was assigned**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.role.created",
      "category": "fleet",
      "description": "A fleet role was created.",
      "default_message": "🏴‍☠️ **A fleet role was created**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.role.updated",
      "category": "fleet",
      "description": "A fleet role was updated.",
      "default_message": "🏴‍☠️ **A fleet role was updated**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "fleet.role.removed",
      "category": "fleet",
      "description": "A fleet role was removed.",
      "default_message": "🏴‍☠️ **A fleet role was removed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "warehouse.stock.changed",
      "category": "warehouse",
      "description": "Guild warehouse stock was created, changed, or removed.",
      "default_message": "📦 **Warehouse stock changed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    },
    {
      "key": "warehouse.reservation.changed",
      "category": "warehouse",
      "description": "A guild warehouse reservation changed.",
      "default_message": "🔒 **Warehouse reservation changed**\n\n{data.summary}\n\nResource: `{resource.type} #{resource.id}`\nEvent: `{event}`\nTriggered by **{actor.display_name}** at `{occurred_at}`."
    }
  ]
}
