# Release 0.15.2 — Seed compatibility hotfix

- Replaces stale `Fortified Ports` references in demo builds with `Reinforced Ports`.
- Migrates legacy upgrade option rows and existing build-slot references to the current name.
- Adds regression coverage so every demo build references only active catalog options.
