{
  "name": "iron-crown-fleet-hub-frontend",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "vite --host 127.0.0.1",
    "dev:default": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:locales": "node scripts/check-locales.mjs",
    "predev": "node scripts/check-env.mjs",
    "prebuild": "node scripts/check-env.mjs",
    "prepreview": "node scripts/check-env.mjs"
  },
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.3"
  }
}
