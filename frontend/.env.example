# Copy to frontend/.env before npm run dev/build.
# For reverse-proxy deployments where API and frontend share one domain, /api is correct.
VITE_API_BASE_URL=/api
# neutral is the safe default. Set to game only for an authorized deployment.
VITE_BUILD_ASSET_MODE=neutral
